[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "exp03-synthetic-augmentation"
version = "0.1.0"
requires-python = ">=3.10,<3.13"

[tool.pytest.ini_options]
pythonpath = [".", "experiments/exp03_synthetic_augmentation"]
testpaths = ["tests"]
